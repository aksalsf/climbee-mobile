import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  input: {
    height: 54,
    margin: 16,
    borderWidth: 1,
    borderRadius: 4,
    padding: 14,
  },
});

export default styles;
